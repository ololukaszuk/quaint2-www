# Binance Real-Time GUI Configuration

# Server port (default: 8753)
API_PORT=8753

# Default trading symbol (default: BTCUSDT)
BINANCE_SYMBOL=BTCUSDT

# Default refresh rate in Hz (default: 10)
REFRESH_RATE=10
